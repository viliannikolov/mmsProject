#include <stdio.h>
 
int main()
 {
// int num=5,quot=(num/2),rem=(num%2);
//      printf("quotient of num(%d) is: %d\nreminder of num(%d) is: %d", num,quot,num,rem);
unsigned char bin;

scanf("%c", &bin);
 printf("%c \n", bin);
    return 0;
 }